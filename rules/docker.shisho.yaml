version: "1"
rules:
  - id: no-latest-tag
    language: dockerfile
    message: |
      `latest` tag is used. Consider to use more concrete tag.
    patterns:
      - FROM :[_]:latest
      - FROM :[_]:latest as :[_]
